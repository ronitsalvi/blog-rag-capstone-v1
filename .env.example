# Blog Q&A Chatbot Environment Variables

# Optional: Admin token for future admin endpoints
ADMIN_TOKEN=change-me-in-production

# Optional: Strict blocklist enforcement (future web fallback feature)  
STRICT_BLOCKLIST=true

# Optional: Ollama host for future abstractive summarization
# OLLAMA_HOST=http://localhost:11434

# Logging level
LOG_LEVEL=INFO